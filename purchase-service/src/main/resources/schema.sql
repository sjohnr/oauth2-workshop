CREATE TABLE purchase
(
    ID              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CASH_CARD_ID    BIGINT NOT NULL,
    DESCRIPTION     VARCHAR(256) NOT NULL,
    AMOUNT          NUMBER NOT NULL,
    OWNER           VARCHAR(256) NOT NULL
);